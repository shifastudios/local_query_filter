name: local_query_filter
description: A type-safe, high-performance Flutter package for client-side filtering, sorting, searching, and pagination of in-memory collections — with isolate-based execution for zero UI jank.
version: 0.2.0
homepage: https://github.com/shifastudios/local_query_filter

environment:
  sdk: ^3.8.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  dart_date: ^1.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

topics:
  - filtering
  - search
  - sort
  - pagination
  - query
  - isolate
  - type-safe
  - in-memory
  - collection
  - dart
  - client-side
